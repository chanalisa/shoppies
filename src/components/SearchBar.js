import React from "react";

const SearchBar = () => {
  return (
    <form>
      <label>
        Movie title
        <input type="text" />
      </label>
    </form>
  );
};

export default SearchBar;
